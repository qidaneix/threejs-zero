export enum EMode {
  select = 'select',
  drawPolyline = 'drawPolyline',
  drawBox = 'drawBox',

  translate = 'translate',
  rotate = 'rotate',
  scale = 'scale',
}

export enum EObject {
  polyline,
  box,
}
